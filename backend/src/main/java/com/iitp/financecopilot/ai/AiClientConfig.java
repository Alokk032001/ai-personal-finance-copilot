package com.iitp.financecopilot.ai;

import org.springframework.context.annotation.Configuration;

/** Marker config for the AI provider seam (exactly one BillExtractor bean). */
@Configuration
public class AiClientConfig {
}
